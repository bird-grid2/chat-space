.side-bar
  .side-bar__user-name
    .top-items
      .top-items__name
        = current_user.name
      .top-items__icons
        = link_to new_group_path, class: "far fa-edit top-items__icons--edit" do
          = icon('far', 'fa-edit')
          %span<>
        = link_to edit_user_path(current_user), class: "fas fa-cog top-items__icons--cog" do
          = icon('fas', 'fa-cog')
  .side-bar__group-list
    - current_user.groups.each do |group|
      .side-bar__group-list__group
        = link_to '#', class: 'group' do
          .side-bar__group-list__group--name
            =group.name
          .side-bar__group-list__group--message
            まだメッセージがありません